# WeDLM Dependencies
# ==================

# Core dependencies (required)
torch>=2.0.0
transformers>=4.30.0
tqdm>=4.65.0
numpy>=1.20.0
safetensors>=0.3.0
xxhash>=3.0.0

# Dataset loading
datasets>=2.12.0

# For distributed evaluation (optional, but needed for wedlm_eval.py)
ray>=2.0.0

# HuggingFace Hub for downloading models
huggingface_hub>=0.14.0

# GPU dependencies (optional - for CUDA acceleration)
# Install these on a GPU-enabled system:
# pip install triton>=2.0.0
# pip install flash-attn>=2.0.0 --no-build-isolation

# Development dependencies (optional)
# accelerate>=0.20.0
